{% extends 'partials/base-error.html.twig' %}

{% block content %}
<main id="hlavni-obsah">
<div class="container my-5">
    <div class="row">
        <div class="col-lg-7 col-md-8 col-12 mx-auto text-center">

            {{ media['theme://img/404.svg'].html(site.title,site.title,'img-fluid mb-4')|raw }}
            <h1>Požadovaná stránka neexistuje</h1>

                <a class="btn btn-primary mt-4" href="{{ home_url }}">Zpět na úvodní stránku</a>
        </div>
    </div>
    

</div>
</main>

{% endblock %}
